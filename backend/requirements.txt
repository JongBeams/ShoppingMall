fastapi==0.115.5
uvicorn==0.32.1
supabase==2.10.0
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
httpx<0.28,>=0.26
redis==5.2.0
python-multipart==0.0.19
aiosmtplib==3.0.2
email-validator==2.2.0
passlib[bcrypt]==1.7.4
PyJWT>=2.10.1
APScheduler==3.10.4
slowapi==0.1.9

# Code Quality & Formatting
black==24.10.0
isort==5.13.2
pylint==3.3.2
mypy==1.13.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0

# PDF 및 RAG Dependencies
pypdf2==3.0.1
sentence-transformers==3.3.1
langchain==0.3.13
langchain-community==0.3.13

# 이미지 AI (CLIP)
transformers==4.47.0
torch==2.5.1
Pillow==11.0.0

# ============================================================
#  프로덕션 의존성 추가
# ============================================================

# 모니터링 (Prometheus + Grafana)
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0

# ONNX 최적화 (CLIP 추론 속도 10배 개선)
onnx==1.16.0
onnxruntime==1.17.1
onnxruntime-gpu==1.17.1  # GPU 사용 시

# 메시지 큐 (비동기 처리)
celery[redis]==5.3.6
flower==2.0.1  # Celery 모니터링 대시보드
kombu==5.3.5  # Celery 메시지 전송

# 부하 테스트
locust==2.20.0

# API Gateway (MSA)
python-consul==1.1.0  # 서비스 디스커버리
grpcio==1.62.1  # gRPC 통신
grpcio-tools==1.62.1  # gRPC 코드 생성

# 로깅 및 에러 추적
sentry-sdk[fastapi]==1.45.0  # 프로덕션 에러 트래킹

# 보안 강화
python-jose[cryptography]==3.3.0  # JWT 고급 기능
cryptography==42.0.5

# 성능 프로파일링
py-spy==0.3.14  # 프로덕션 프로파일러
memory-profiler==0.61.0