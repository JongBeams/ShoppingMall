

---

## 1. 실시간 원격지원 (가장 특별함) ⭐⭐⭐⭐⭐

### 사용 기술
- **WebRTC**: 화면 공유 (P2P 스트리밍)
- **WebSocket**: 실시간 1:1 채팅 + 시그널링 + 원격 제어 명령 전송
- **STUN 서버**: NAT 통과 (Google STUN 사용)

### 동작 방식

#### [1단계: 실시간 1:1 채팅]

**[채팅방 생성 및 연결]**
1. 사용자가 "상담사 연결" 버튼 클릭
2. REST API로 채팅방 생성 (`POST /chat/rooms`)
3. Supabase `chat_rooms` 테이블에 저장 (status: `waiting`)
4. 관리자에게 WebSocket으로 새 채팅 알림 전송

**[WebSocket 양방향 통신]**
1. 사용자: `ws://localhost:8000/chat/ws/{room_id}` 연결
2. 관리자: 동일한 `room_id`로 WebSocket 연결
3. ConnectionManager가 채팅방별 연결 관리 (딕셔너리 구조)

**[메시지 교환]**
1. 메시지 전송 시 Supabase `chat_messages` 테이블에 저장
2. WebSocket으로 같은 채팅방의 모든 연결자에게 브로드캐스트
3. 메시지 히스토리 자동 로드 (채팅방 재접속 시)

---

#### [2단계: 화면 공유 및 원격 제어]

**[사용자 측 - 화면 공유 시작]**
1. 관리자가 "원격 승인 요청" 전송 → 사용자 화면에 승인 버튼 표시
2. 사용자 승인 시 `getDisplayMedia()`로 현재 브라우저 화면 캡처
3. RTCPeerConnection 생성 (STUN 서버 설정)
4. WebRTC Offer 생성 후 WebSocket으로 관리자에게 전송
5. Answer/ICE Candidate 수신하여 P2P 연결 완료
6. 화면 스트림을 관리자에게 실시간 전송

**[관리자 측 - 화면 수신 및 제어]**
1. WebRTC Offer 수신 (사용자로부터 WebSocket을 통해)
2. RTCPeerConnection 생성 및 Answer 전송
3. `ontrack` 이벤트로 원격 비디오 스트림 수신
4. `<video>` 요소에 스트림 연결하여 실시간 화면 표시
5. 화면에서 클릭/스크롤 위치 계산
6. WebSocket으로 제어 명령 전송 (좌표, 이벤트 타입)

**[사용자 측 - 원격 제어 수신]**
1. 관리자의 클릭 좌표 수신
2. `document.elementFromPoint(x, y)`로 해당 위치의 DOM 요소 찾기
3. 실제 마우스 이벤트 생성 및 전송 (mousedown, mouseup, click)
4. 입력 필드면 자동 포커스
5. 키보드 입력도 동일한 방식으로 재현

### 왜 특별한가?
- **쇼핑몰에서 거의 없는 기능**: 대부분 챗봇이나 텍스트 상담만 제공
- **기술 난이도 높음**: WebRTC P2P 연결, 시그널링, DOM 이벤트 재현
- **실용성**: 노인층, 비IT 사용자 고객 지원에 매우 유용

### **구현 파일**
- `frontend/app/components/chat/LiveChat.tsx` (사용자: 화면 공유 + 원격 제어 수신)
- `frontend/app/crm/live-chat/page.tsx` (관리자: WebRTC 수신 + 화면 보기 + 제어)
- `backend/app/routers/chat.py` (WebSocket 시그널링 서버)

---

## 2. AI 챗봇 (RAG + 개인화 추천) ⭐⭐⭐⭐

### 2-1. RAG (문서 검색)

#### 사용 기술
- **임베딩 모델**: BAAI/bge-m3 (SentenceTransformer)
- **벡터 DB**: Supabase pgvector (PostgreSQL 확장)
- **LLM**: Ollama qwen2.5:14b (로컬 LLM)

#### 동작 방식

**[문서 업로드]**
1. PDF 텍스트 추출 (PyPDF2)
2. 청크 분할 (500자 단위, 50자 오버랩)
   - 문장 단위로 자르기 (마침표/줄바꿈 기준)
3. bge-m3로 각 청크를 1024차원 벡터로 임베딩
4. pgvector에 저장 (content + embedding)

**[검색]**
1. 사용자 질문을 bge-m3로 임베딩 (1024차원 벡터)
2. pgvector의 코사인 유사도 검색 (RPC 함수 호출)
3. 유사도 높은 상위 3개 문서 청크 반환

**[답변 생성]**
1. 검색된 문서 + 사용자 질문을 프롬프트로 구성
2. Ollama API 호출 (스트리밍)
3. 토큰 단위로 실시간 답변 생성

#### 왜 특별한가?
- **임베딩 모델 직접 사용**: bge-m3는 다국어 지원 고성능 모델
- **벡터 검색 DB**: pgvector로 확장 가능한 아키텍처
- **로컬 LLM**: API 비용 없이 자체 운영 가능

#### 구현 파일
- `backend/app/routers/documents.py` (PDF 업로드 및 임베딩)
- `backend/app/services/rag_search.py` (검색 및 답변 생성)

---

### 2-2. 개인화 추천 (구매 패턴 분석)

#### 분석 항목
1. **선호 태그**: 구매한 상품 태그 빈도 분석 (상위 10개)
2. **가격대**: 구매 상품의 최소/최대/평균 가격
3. **구매 주기**: 주문 간격 평균 (weekly, monthly, quarterly, occasional)
4. **재구매 상품**: 2회 이상 구매한 상품 목록
5. **계절 선호도**: 봄/여름/가을/겨울 구매 패턴

#### 추천 알고리즘
1. **최근 6개월 구매 이력** 조회
2. **선호 태그 상위 5개** 추출
3. 전체 상품 중 태그 매칭 및 매칭 점수 계산
   - 태그 하나 일치할 때마다 +1점
4. **가격대 필터링** (평균 가격 ±30% 범위, 설정 가능)
5. **정렬**: 매칭 점수 > 평점 > 판매량
6. **구매한 상품 제외** (선택적)

#### LLM 개인화
- 구매 패턴 분석 결과를 프롬프트에 포함
- "고객님께서 평소 노트북을 선호하시는데..." 같은 개인화된 멘트 생성
- 재구매 상품 언급 ("지난번 구매하신 XX와 비슷한...")
- 매칭 점수 기반 추천 이유 설명

#### 왜 특별한가?
- **단순 추천이 아닌 패턴 분석**: 태그/가격/주기 종합 분석
- **LLM 통합**: 추천 이유를 자연어로 설명
- **실시간 개인화**: 로그인 시 즉시 맞춤 추천

#### 구현 파일
- `backend/app/services/personalized_recommendation.py` (패턴 분석 및 추천)
- `backend/app/services/product_statistics.py` (상품 통계)

---

### 2-3. 통합 플로우 (스마트 챗)

1. **문서 검색**: 사용자 질문 관련 문서 3개 찾기 (RAG)
2. **키워드 추출**: 불용어 제거 후 핵심 키워드 추출
3. **상품 추천 분기**:
   - 키워드 있으면 → 태그 검색 (50개)
   - "추천해줘" 같은 질문 + 로그인 → 개인화 추천 (50개)
   - 나머지 → 신상품 (50개)
4. **LLM 답변 생성**: 문서 + 상품 정보를 통합한 프롬프트 (스트리밍)
5. **필터링**: LLM이 답변에서 언급한 상품만 최종 반환 (최대 10개)

#### 왜 특별한가?
- **RAG + 상품 추천 통합**: 문서 검색 + 쇼핑 기능 결합
- **컨텍스트 인식**: 질문 의도에 따라 다른 추천 로직 적용
- **LLM 필터링**: AI가 직접 선택한 상품만 보여줌

#### 구현 파일
- `backend/app/routers/chat.py` (스마트 챗 API)

---

## 3. AI 선물 추천 마법사 ⭐⭐⭐

### 사용 기술
- **LLM**: Ollama qwen2.5:14b (구조화된 JSON 응답 생성)
- **다단계 필터링**: 관계/관심사/스타일/가격대 기반
- **감성 AI**: 선물 메시지 3가지 톤 생성

### 동작 방식

#### [1단계: 대화형 질문 플로우]
1. **7가지 질문** (채팅 인터페이스):
   - 누구에게? (연인/부모/친구/동료 등)
   - 연령대? (10대~50대+)
   - 스타일? (미니멀/화려한/빈티지/모던/캐주얼)
   - 관심사? (운동/독서/게임/여행 등, 다중 선택)
   - 목적? (생일/기념일/축하/위로/감사)
   - 예산? (~3만원/3~10만원/10~30만원/30만원+)
   - 특별 요청? (각인 가능/실용적/의미있는/유니크한)

#### [2단계: 스마트 상품 필터링]
1. **관계 기반 카테고리 매핑**:
   - 연인 → 주얼리, 향수, 가방, 패션잡화
   - 부모 → 건강식품, 가전, 홈인테리어
   - 친구 → 뷰티, 디지털, 문구
   - 동료 → 사무용품, 생활용품, 식품

2. **관심사 반영**:
   - 운동 → 스포츠용품, 운동복, 피트니스
   - 독서 → 북 라이트, 북마크, 독서대
   - 게임 → 게이밍 기어, 굿즈
   - 여행 → 캐리어, 여행용품

3. **가격대 필터링**: 예산 ±20% 범위
4. **품질 필터**: 평점 4.0 이상, 재고 있는 것만
5. **정렬**: 스타일 매칭 > 평점 > 리뷰 수

#### [3단계: AI 추천 및 이유 설명]
LLM이 상위 50개 상품 중 **최적 3개 선택** 후:

1. **추천 이유 3가지** (구조화)

2. **선물 메시지 3가지** (감성/위트/진지)

3. **주의사항**

4. **포장 팁**

5. **전달 방법**

#### [4단계: 선물 히스토리 관리]
- **과거 선물 저장**: 누구에게, 무엇을, 언제, 만족도
- **재구매 방지**: "작년 생일엔 팔찌 줬으니 올해는 목걸이"
- **패턴 학습**: "이 사람은 주로 악세사리 선호"
- **기념일 알림**: D-30 자동 알림

### 왜 특별한가?
- **국내 쇼핑몰 최초급**: 대화형 AI 선물 추천 + 감성 메시지 생성
- **비즈니스 가치**:
  - 선물 고민 시간 단축 (5분 내)
  - 구매 전환율 증가 (확신 증가)
  - 객단가 상승 (선물은 고가 선택 경향)
  - 재방문 유도 (기념일 알림)
- **UX 혁신**: 채팅 스타일 질문 → 감성적 디자인 → 즉시 추천

### **구현 파일**
- `frontend/app/components/gift-wizard/GiftWizardChat.tsx` (대화형 UI)
- `frontend/app/components/gift-wizard/GiftWizardResult.tsx` (결과 페이지)
- `backend/app/routers/gift_wizard.py` (API)
- `backend/app/services/gift_filtering.py` (필터링 로직)
- `backend/app/services/gift_llm.py` (LLM 추천 생성)

---

## 4. CLIP 기반 이미지 검색 ⭐⭐⭐⭐

### 사용 기술
- **CLIP 모델**: OpenAI CLIP-ViT-B/32 (Vision Transformer)
- **벡터 DB**: Supabase pgvector (PostgreSQL 확장)
- **딥러닝 프레임워크**: PyTorch + HuggingFace Transformers
- **이미지 처리**: PIL (Python Imaging Library)

### 동작 방식

#### [1단계: 상품 이미지 임베딩 생성]
1. **상품 등록 시 자동 임베딩**:
   - 상품 이미지 URL에서 이미지 다운로드
   - CLIP Processor로 이미지 전처리 (정규화, 리사이징)
   - CLIP Vision Encoder로 512차원 벡터 생성
   - L2 정규화 (코사인 유사도 계산 준비)

2. **벡터 저장**:
   - Supabase `products` 테이블의 `image_embedding` 컬럼에 저장
   - pgvector 타입 (`vector(512)`)으로 인덱싱

#### [2단계: 이미지 업로드 검색]
1. **사용자가 이미지 업로드**:
   - 드래그 앤 드롭 또는 파일 선택으로 이미지 업로드
   - 지원 형식: JPG, PNG (최대 5MB)

2. **검색 이미지 임베딩 생성**:
   - 업로드된 이미지를 바이트 스트림으로 읽기
   - 동일한 CLIP 모델로 512차원 벡터 생성
   - L2 정규화 적용

3. **벡터 유사도 검색**:
   - pgvector의 `match_products_by_image` RPC 함수 호출
   - 코사인 유사도 기반 검색 (내적으로 계산)
   - 유사도 임계값: 0.3 (30% 이상)
   - 상위 5개 상품 반환

4. **결과 표시**:
   - 유사도 점수와 함께 상품 카드 표시
   - 유사도 배지 (`72% 유사` 등)
   - 상품명, 가격, 평점, 카테고리 정보 포함

### 기술적 특징

#### CLIP 모델의 강점
- **멀티모달 학습**: 텍스트와 이미지를 같은 공간에 임베딩
- **제로샷 학습**: 사전 학습된 모델로 추가 학습 없이 바로 사용
- **의미론적 유사도**: 단순 픽셀 비교가 아닌 시각적 개념 이해
  - 예: "빨간 가방" 이미지 → 비슷한 색상/스타일의 가방 검색

#### pgvector 벡터 검색
```sql
CREATE OR REPLACE FUNCTION match_products_by_image(
  query_embedding vector(512),
  match_threshold float,
  match_count int
)
RETURNS TABLE (
  id uuid,
  name text,
  price numeric,
  similarity float
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT
    products.id,
    products.name,
    products.price,
    1 - (products.image_embedding <=> query_embedding) AS similarity
  FROM products
  WHERE
    products.is_active = true
    AND products.image_embedding IS NOT NULL
    AND 1 - (products.image_embedding <=> query_embedding) > match_threshold
  ORDER BY products.image_embedding <=> query_embedding
  LIMIT match_count;
END;
$$;
```

- **인덱싱**: IVFFlat 또는 HNSW 인덱스로 빠른 검색
- **확장성**: 수십만 개 상품도 밀리초 내 검색 가능
- **정확도**: 코사인 유사도로 정밀한 매칭

### 실사용 예시

#### 사용 케이스 1: 길거리에서 본 옷 찾기
1. 지나가는 사람이 입은 코트 사진 촬영
2. 앱에 업로드
3. 비슷한 스타일/색상의 코트 상품 5개 추천
4. 유사도 순으로 정렬 (85%, 78%, 72%, 65%, 61%)

#### 사용 케이스 2: SNS에서 본 악세사리 검색
1. 인스타그램 스크린샷 업로드
2. CLIP이 이미지에서 악세사리 인식
3. 유사한 디자인의 목걸이/귀걸이 검색
4. 가격/평점 비교 후 구매

#### 사용 케이스 3: 기존 상품과 비슷한 것 찾기
1. 이미 구매한 신발 사진 업로드
2. 다른 색상/브랜드의 유사 디자인 검색
3. 재구매 또는 선물용으로 활용

### 왜 특별한가?

#### 기술적 난이도
- **딥러닝 모델 통합**: CLIP 모델 서빙 및 최적화
- **벡터 DB 운영**: pgvector 인덱싱 및 쿼리 최적화
- **대용량 이미지 처리**: 효율적인 임베딩 생성 파이프라인

#### 차별화 포인트
- **국내 중소 쇼핑몰에서 드문 기능**: 대형 쇼핑몰(무신사, 지그재그) 수준
- **텍스트 검색의 한계 극복**: "빨간 가죽 가방"을 검색어로 설명하기 어려울 때
- **UX 혁신**: 이미지 하나로 즉시 검색 (타이핑 불필요)

#### 비즈니스 임팩트
- **구매 전환율 증가**: 원하는 상품을 쉽게 찾을 수 있음
- **체류 시간 증가**: 유사 상품 탐색 과정에서 추가 상품 발견
- **모바일 친화적**: 카메라로 바로 촬영해서 검색 가능
- **SNS 시대 맞춤**: 인스타/틱톡에서 본 제품 즉시 검색

### 기술 스택 세부사항

#### GPU 가속
- CUDA 사용 가능 시 GPU에서 임베딩 생성 (10배 빠름)
- CPU 폴백 지원 (약 500ms/이미지)

#### 모델 최적화
- 싱글톤 패턴으로 모델 한 번만 로드
- `torch.no_grad()` + `model.eval()`로 추론 최적화
- 배치 처리 가능 (여러 이미지 동시 임베딩)

#### 에러 핸들링
- 이미지 포맷 검증 (MIME type 체크)
- 파일 크기 제한 (5MB)
- RGB 변환 (알파 채널 제거)
- 타임아웃 설정 (네트워크 다운로드)

### **구현 파일**
- `backend/app/services/image_embedding.py` (CLIP 임베딩 생성)
- `backend/app/routers/product.py` (이미지 검색 API: `/search-by-image`)
- `frontend/app/image-search/page.tsx` (이미지 업로드 UI + 검색 결과)
- `backend/regenerate_embeddings.py` (기존 상품 일괄 임베딩 스크립트)

---

## 5. AI 리뷰 요약 ⭐⭐

### 사용 기술
- Ollama qwen2.5:14b (스트리밍)

### 동작 방식
1. 최근 리뷰 30개 조회 (토큰 제한 고려)
2. 평점 분포 계산 (5점~1점 각각 몇 개)
3. 구조화된 프롬프트 생성:
   - 전체 평가
   - 주요 장점 (3가지)
   - 단점 및 개선점
   - 구매 시 참고사항
4. Ollama 스트리밍으로 실시간 요약 생성

### 왜 특별하지 않나?
- **요새 많은 쇼핑몰에서 사용**: 쿠팡, 네이버쇼핑 등 이미 적용
- **기술적으로 단순**: 프롬프트만 잘 짜면 끝
- **차별화 없음**: 표준 기능

### **구현 파일**
- `backend/app/routers/reviews.py` (리뷰 요약 API)

---

## 포트폴리오 강조 순서

### 1순위: 실시간 원격지원 
- **기술 난이도**: ⭐⭐⭐⭐⭐
- **차별화**: ⭐⭐⭐⭐⭐ (쇼핑몰에 거의 없음)
- **복잡도**: WebRTC + WebSocket + DOM 이벤트 재현
- **강조 포인트**:
  - P2P 화면 공유 구현
  - 실시간 양방향 제어
  - 고객 지원 혁신

### 2순위: CLIP 기반 이미지 검색
- **기술 난이도**: ⭐⭐⭐⭐⭐
- **차별화**: ⭐⭐⭐⭐ (중소 쇼핑몰에서 드문 기능)
- **복잡도**: CLIP 모델 통합 + pgvector 벡터 검색 + GPU 최적화
- **강조 포인트**:
  - OpenAI CLIP Vision Transformer 사용
  - 512차원 벡터 임베딩 + 코사인 유사도 검색
  - 텍스트 검색의 한계 극복 (이미지만으로 검색)
  - GPU 가속 및 배치 처리 최적화
  - 멀티모달 AI 활용

### 3순위: RAG 기반 AI 챗봇 + 개인화 추천
- **기술 난이도**: ⭐⭐⭐⭐
- **차별화**: ⭐⭐⭐⭐ (구매 패턴 분석 포함)
- **복잡도**: 임베딩 모델 + pgvector + 패턴 분석 알고리즘
- **강조 포인트**:
  - bge-m3 임베딩 + 벡터 검색
  - 구매 이력 기반 개인화 알고리즘
  - RAG + 상품 추천 통합

### 4순위: AI 선물 추천 마법사
- **기술 난이도**: ⭐⭐⭐
- **차별화**: ⭐⭐⭐⭐ (국내 쇼핑몰 최초급)
- **복잡도**: 다단계 필터링 + LLM 추천 + 감성 메시지 생성
- **강조 포인트**:
  - 대화형 UX (7단계 질문)
  - 관계/관심사/스타일 기반 필터링
  - 감성 메시지 3가지 톤 자동 생성
  - 선물 히스토리 & 기념일 관리

### 5순위: AI 리뷰 요약
- **기술 난이도**: ⭐⭐
- **차별화**: ⭐⭐ (흔한 기능)
- **복잡도**: 프롬프트 엔지니어링
- **강조 포인트**:
  - 부가 기능으로 간단히 언급
  - "추가로 AI 리뷰 요약도 구현했습니다" 정도

---

## 포트폴리오 설명 예시

### 실시간 원격지원
> "고객이 어려워하는 주문 과정을 관리자가 **실시간으로 화면을 보며 원격 제어**할 수 있는 기능입니다. WebRTC로 P2P 화면 공유를 구현하고, WebSocket으로 클릭/스크롤/키보드 이벤트를 전송하여 실제 DOM을 조작합니다. 쇼핑몰에서 보기 드문 기술로, 노인층이나 비IT 사용자 지원에 효과적입니다."

### CLIP 기반 이미지 검색
> "OpenAI의 **CLIP Vision Transformer 모델**을 활용한 이미지 기반 상품 검색 시스템입니다. 사용자가 업로드한 이미지를 512차원 벡터로 임베딩하고, pgvector로 코사인 유사도 검색을 수행하여 시각적으로 유사한 상품을 찾습니다. '빨간 가죽 가방'처럼 텍스트로 설명하기 어려운 상품을 이미지 하나로 즉시 검색할 수 있어, SNS에서 본 제품이나 길거리에서 촬영한 옷을 바로 찾을 수 있습니다. GPU 가속과 싱글톤 패턴으로 최적화하여 실시간 검색이 가능합니다."

### AI 챗봇 + 개인화 추천
> "사용자 질문에 대해 **RAG 검색**(bge-m3 임베딩 + pgvector)과 **구매 패턴 분석**을 통합한 AI 챗봇입니다. 최근 6개월 구매 이력을 분석하여 선호 태그, 가격대, 구매 주기를 파악하고, 매칭 점수 알고리즘으로 맞춤 상품을 추천합니다. LLM이 '고객님께서 평소 XX를 선호하시는데...' 같은 개인화된 답변을 생성합니다."

### AI 선물 추천 마법사
> "7가지 질문(관계/연령/스타일/관심사/목적/예산/요청)에 답하면, AI가 다단계 필터링(관계→카테고리 매핑, 스타일→키워드 매칭, 가격대 필터)을 통해 최적 선물 3개를 추천합니다. 특히 LLM이 추천 이유 3가지와 함께 감성적/위트있는/진지한 톤의 선물 메시지 3가지를 자동 생성하여, 선물 고민 시간을 5분 내로 단축시킵니다. 선물 히스토리 관리와 기념일 D-30 알림 기능으로 재방문을 유도합니다."

### AI 리뷰 요약
> "추가로 Ollama를 활용한 AI 리뷰 요약 기능도 구현했습니다."
